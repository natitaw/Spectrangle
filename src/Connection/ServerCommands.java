package Connection;

public class ServerCommands {

    public static void clientConnects(String[] args){
        String name = args[0];



        // Check if name is duplicate of existing name

        // Set name

        //Check the second argument, if it exists
        if (args.length >= 2) {
            if (args[1].equals("chat") ){
                // Enable chat
            }
        }

        // Put client in lobby




    }
}
